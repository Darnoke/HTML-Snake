import { FieldColorPipe } from './field-color.pipe';

describe('FieldColorPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldColorPipe();
    expect(pipe).toBeTruthy();
  });
});
